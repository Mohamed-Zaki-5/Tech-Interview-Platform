# Module boundaries

Each directory is an independently owned domain or platform boundary inside the modular monolith. Phase 1 establishes ownership only; domain HTTP routes and use cases are added in later phases.
